function tryRotate(_, flip, _, _, oldFlip)
  if flip ~= oldFlip then
    local success = self.Book.setPage(self.Book.getPage() + 1)
    if not success then
      self.Book.setPage(0)
    end
    return false
  end
  return true
end
