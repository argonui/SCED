i18n = {}

function i18n.getById(id)
  if I18N_DATA[id] then
    local val = I18N_DATA[id][optionPanel.uiLanguage]
    if val and val ~= "" then
      return val
    end
    return I18N_DATA[id].en or id
  end
  return id
end
